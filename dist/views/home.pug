block styles
      link(rel="stylesheet", type="text/css", href="./styles/home.css")

block content 

h1 Tous les messages !

if user && user.messages && user.messages.length > 0
      each message in user.messages
            p(style='color: black;')= message.user_message

form(action='/contact')
      button(type='submit' class='button') nous contacter
